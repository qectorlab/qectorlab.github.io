# qector.store — canonical crawler policy (audit S3: minimal, widely supported
# directives). Everything is allowed; the wildcard group is the only enforceable
# rule most crawlers need. Vendor-specific agent names were removed because a
# matching named group disables the wildcard group for that agent and adds
# nothing over "Allow: /". The AI-crawler policy statement lives in ai.txt,
# the machine-readable discovery surface lives in llms.txt / llms-full.txt.

User-agent: *
Allow: /
Disallow: /success

Sitemap: https://qector.store/sitemap.xml
